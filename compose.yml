services:
  yarp:
    image: simpleproxy:v1
    build: .
    volumes:
      - ./appSettings.json:/app/appSettings.json:ro
      - ./certificate/keys:/app/keys:ro
    restart: 'unless-stopped'
    network_mode: host
